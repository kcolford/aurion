#!/bin/sh
read -r url < global-url
dir=build/repo
mkdir -p $dir
rsync -lru "$url"/ $dir
for repo in $dir/*.db; do
    if repo="$(readlink -e "$repo")"; then
	repo-add -n -R "$repo"  $dir/*.pkg.tar*
    fi
done
rsync -lr --delete $dir/ "$url"
