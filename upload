#!/bin/sh
url=root@zeus:/srv/http/aurion	# configure this

dir=build/repo
mkdir -p $dir
rsync -lr "$url"/ $dir
for repo in $dir/*.db; do
    repo="$(readlink -f "$repo")" && repo-add -d -n -R "$repo"  $dir/*.pkg.tar*
done
rsync -lr --delete $dir/ "$url"
